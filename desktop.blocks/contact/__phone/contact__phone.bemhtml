block('contact').elem('phone')(
    content()(function() {
        return {
            block: 'button',
            mix: { block: 'contact', elem: 'phone-button' },
            content: this.ctx.content
        };
    })
);
