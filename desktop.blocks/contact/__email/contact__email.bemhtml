block('contact').elem('email')(
    content()(function() {
        return {
            block: 'link',
            mix: { block: 'contact', elem: 'email-link' },
            url: 'mailto:' + this.ctx.content,
            content: this.ctx.content
        };
    })
);
