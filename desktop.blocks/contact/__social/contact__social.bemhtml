block('contact').elem('social')(

    tag()('a'),

    attrs()(function() {
        return { href: this.ctx.url || '#' };
    }),

    content()(function() {
        return this.ctx.service
    })

);
